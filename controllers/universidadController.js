const { validationResult } = require('express-validator');
const Universidad = require('../models/universidadModel');

exports.createUniversidad = async (req, res) => {
  // Aquí maneja la lógica para crear una universidad
};

// Otros controladores según sea necesario (por ejemplo, obtener universidades, actualizar universidades, eliminar universidades, etc.)
